# Frameworks 绿色环境 npm 配置（国内镜像）
# 权威副本: %FRAMEWORKS_HOME%\Config\npm\.npmrc
# dev-shell 通过 npm_config_registry/npm_config_cache 直接生效。
# 该文件作为配置参考；会话实际值由环境变量提供。

registry=https://registry.npmmirror.com
cache=${FRAMEWORKS_HOME}/Caches/npm
